console.log("BannerJS ok");

// document.addEventListener("DOMContentLoaded", function () {
//   const photoGrid = document.getElementById("photo-grid");
//   const photoItems = document.querySelectorAll(".photo-item");
//   let currentIndex = 0;

//   function showRandomPhoto() {
//     if (photoItems.length === 0) return;

//     // Masquer toutes les photos
//     photoItems.forEach((item) => (item.style.display = "none"));

//     // Afficher une photo aléatoire
//     currentIndex = Math.floor(Math.random() * photoItems.length);
//     photoItems[currentIndex].style.display = "block";
//   }

//   // Changer de photo toutes les 5 secondes
//   setInterval(showRandomPhoto, 5000);

//   // Afficher la première photo immédiatement
//   showRandomPhoto();
// });
